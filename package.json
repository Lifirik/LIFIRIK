{
  "name": "lifirik",
  "version": "1.0.0",
  "description": "LIFIRIK - a dynamic, repeating mechanism governed solely by the physical laws of force and motion",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "server.js",
  "//files": "Allowlist, not a blocklist: whatever bundles this (npm pack, a deploy step, a container build) gets exactly what the app needs to run and nothing else.",
  "files": [
    "server.js",
    "auth.mjs",
    "storage.mjs",
    "ratelimit.mjs",
    "db-file.mjs",
    "public/",
    "scripts/build.mjs",
    "scripts/seed-official-levels.js",
    "scripts/campaign-seed.json",
    "LICENSE",
    "README.md",
    "Instructions.md"
  ],
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.mjs",
    "seed": "node scripts/seed-official-levels.js"
  },
  "dependencies": {
    "express": "^4.22.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
